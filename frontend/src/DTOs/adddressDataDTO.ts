interface addressDataDTO {
  id: number | null,
  cep: string | null,
  street: string | null,
  neighborhood: string | null,
  city: string | null,
  state: string | null,
}

export type { addressDataDTO }